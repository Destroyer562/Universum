local StarterGui = game:GetService("StarterGui")StarterGui:SetCoreGuiEnabled(Enum.CoreGuiType.Health, true)
--<This should work, if it does not work it is simply because I used the incorrect term "Health", it which it should be 
"Health Bar or something else. >--
